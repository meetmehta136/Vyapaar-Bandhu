# Discrepancy Report — Phase 1 Audit
**Generated:** 2026-06-01
**Source-of-truth review of `ml/`, `backend/`, and `docs/` files**

Each item is numbered `D-NNN`. Severity: 🔴 critical (interview/credibility), 🟡 accuracy, 🟢 minor.

---

## [ID] D-001 🔴
**Claim (CONTINUATION file says):** "37% overall recall@1, 92% on indexed sections (11/12)"
**Reality:** `ml/evaluation/benchmark_report.json` → `avg_section_recall: 0.2`, `section_recall_at_1: 6`, `avg_keyword_recall: 0.242`
**File:** `ml/evaluation/benchmark_report.json` lines 3-6
**Note:** The README.md and `ml/models/gstmind-embedding-model/README.md` already state the correct numbers (20% / 6/30). Only the continuation file is wrong.
**Fix required:** Update the continuation context file's "KEY NUMBERS TO MEMORIZE" and "INTERVIEW STORY" sections to use 20% / 6/30, and replace the false "92% on indexed sections" claim with the actual value.
**Status:** OPEN

---

## [ID] D-002 🔴
**Claim:** "Production uses base e5-small (better performance)" — multiple docs.
**Reality:** `backend/app/services/gstmind_index.py:12` → `DEFAULT_MODEL = "meet136/gst-legal-embeddings-v1"` (the regressed HF model, NOT the base).
**File:** `backend/app/services/gstmind_index.py` line 12
**Fix required:** Either change `DEFAULT_MODEL` to `"intfloat/multilingual-e5-small"` (matches docs), or update docs to say "production uses the fine-tuned HF model." Since benchmark shows base model is better, the recommended fix is changing the code default.
**Status:** OPEN

---

## [ID] D-003 🔴
**Claim (multiple docs):** "MuRIL fine-tuned classifier"
**Reality:** `ml/train.py:77` trains `google/muril-base-cased` (MuRIL), but the model published on HF and called from production is `meet136/indicbert-gst-classifier` — these are TWO DIFFERENT models. The HF upload target in `prepare_dataset.py:202` is `meet136/indian-gst-transaction-classification` (a third name). The `inference_config.json` in `ml/train.py:637` writes `model_name: "meet136/muril-gst-classifier-v2"` (a fourth name).
**Files:** `ml/train.py:77, 637`; `backend/app/services/classification_service.py:112`; `ml/data/prepare_dataset.py:202`
**Fix required:** Pick ONE canonical HF repo name, update all references. Recommended: align to the trained model — use `meet136/muril-gst-classifier-v2` and rename local dir accordingly. Update `classification_service.py:112` to call this name, and fix the `_classify_your_model` HF repo to match.
**Status:** OPEN

---

## [ID] D-004 🟡
**Claim:** Model is "fine-tuned on 6300 synthetic Indian SME transactions" / "7,229 total"
**Reality:** `ml/data/dataset_stats.json` confirms 7,229 total, 5,353 train. ✓ correct.
**Files:** `ml/data/dataset_stats.json`; `ml/data/prepare_dataset.py:202`
**Fix required:** No number change needed, but verify the HF dataset repo `meet136/indian-gst-transaction-classification` was actually uploaded (cannot verify without HF API access). Note in KNOWN_LIMITATIONS.
**Status:** OPEN (verify HF upload)

---

## [ID] D-005 🔴
**Claim (multiple):** "Claude-generated multilingual synthetic data"
**Reality:** `ml/data/active_provider.txt` = `Template`. The `generate_dataset.py` falls back to a template-based generator when LLM providers fail. The `generation_progress.json` file shows batches with all three numbers (presumably Claude/DeepSeek/Gemini counts) — but the active provider at end of run is `Template`, meaning **all 7,229 rows were likely generated by templates, not by Claude.**
**Files:** `ml/data/active_provider.txt`; `ml/data/generation_progress.json`; `ml/data_pipeline/generate_dataset.py:385-432`
**Fix required:** Honestly describe data as "template-based multilingual synthetic dataset" (rules + multilingual tokens). Update interview story.
**Status:** OPEN

---

## [ID] D-006 🔴
**Claim (`gstmind_index.py:10-11`):** "v2 model — MRR@10: 0.75 (+357% over base)"
**Reality:** `ml/evaluation/benchmark.py` does NOT compute MRR@10 — only section_recall and keyword_recall. The MRR@10 number is fabricated/unverifiable.
**File:** `backend/app/services/gstmind_index.py:10-12`
**Fix required:** Either (A) add MRR@10 to `benchmark.py` and re-run to get the actual number, or (B) delete the false comment. Option A is the rigorous fix.
**Status:** OPEN

---

## [ID] D-007 🟡
**Claim:** "98 CGST sections parsed (out of 174)"
**Reality:** `ml/data/parsed_acts/` has **99 files** (one PDF, ~98 section JSONs — see "98 parsed sections" claim in `docs/PROGRESS.md:429`). This number is consistent across docs. ✓ mostly correct.
**File:** `docs/PROGRESS.md:429`
**Fix required:** None — claim is accurate.
**Status:** VERIFIED

---

## [ID] D-008 🔴
**Claim:** "994 ChromaDB chunks (973 CGST + 21 circulars)"
**Reality:** `ml/data/processed/all_chunks.jsonl` has **994 lines** ✓ correct.
**File:** `ml/data/processed/all_chunks.jsonl`
**Fix required:** None — verified.
**Status:** VERIFIED

---

## [ID] D-009 🔴
**Claim (interview story):** "Section 17(5) = the blocked ITC categories"
**Reality:** `backend/app/services/compliance_engine.py` (and `compliance/itc/{category}` endpoint) implements Section 17(5) rules. ✓ correct.
**File:** `backend/app/services/compliance_engine.py`
**Fix required:** None.
**Status:** VERIFIED

---

## [ID] D-010 🔴
**Claim:** "3-tier classifier (keyword → BART → MuRIL)"
**Reality:** `backend/app/services/classification_service.py:34-60` implements 3 tiers: keyword (Layer 1), BART-mnli (Layer 2), and a custom HF model (Layer 3) called `meet136/indicbert-gst-classifier`. ✓ structure is correct, model name is wrong (see D-003).
**File:** `backend/app/services/classification_service.py:34-60`
**Fix required:** Update model name (covered in D-003).
**Status:** VERIFIED (structure)

---

## [ID] D-011 🔴
**Claim:** "8 database tables"
**Reality:** `docs/PROGRESS.md:257-258` and `README.md:376-384` list 8 tables: `ca_partners, users, transactions, invoices, gst_ledger, filing_history, alerts, gstr2b_cache`. ✓ correct.
**Files:** `backend/alembic/versions/001_initial.py`; `README.md:376-384`
**Fix required:** None — verified.
**Status:** VERIFIED

---

## [ID] D-012 🔴
**Claim (CONTINUATION file):** "480 QA pairs used for fine-tuning"
**Reality:** The continuation file and `ml/models/gstmind-embedding-model/README.md:38` say 480. But `docs/PROGRESS.md:48` and `README.md:411` say 4970 (8 epochs v2). These two numbers refer to different models — the HF model card is for v1 (480 pairs), the README is for v2 (4970 pairs). Both are technically correct depending on context, but the continuation file is ambiguous.
**Files:** `ml/models/gstmind-embedding-model/README.md:38`; `docs/PROGRESS.md:39-48`; `README.md:411`
**Fix required:** Clarify: "v1 was 480 pairs (regressed), v2 was 4970 pairs (also regressed, both v1 and v2 underperform base)."
**Status:** OPEN

---

## [ID] D-013 🟡
**Claim (CONTINUATION file):** "89 tests + 14 new = 103 total"
**Reality:** 8 test files in `backend/tests/`. Counts need verification.
**File:** `backend/tests/`
**Fix required:** Run `pytest --collect-only -q` to get exact count.
**Status:** OPEN (verify count)

---

## [ID] D-014 🟡
**Claim:** "21 circulars (not 30)" / "21 CBIC circulars"
**Reality:** `ml/data/metadata/` has **21 circular_*.json files**. ✓ correct.
**File:** `ml/data/metadata/`
**Fix required:** None.
**Status:** VERIFIED

---

## [ID] D-015 🟡
**Claim (CONTINUATION file):** "98/174 sections parsed"
**Reality:** 99 files in `ml/data/parsed_acts/` (1 PDF + ~98 section JSONs).
**File:** `ml/data/parsed_acts/`
**Fix required:** None — close enough.
**Status:** VERIFIED

---

## [ID] D-016 🔴
**Claim (CONTINUATION file):** "97/174 sections parsed" in one place
**Reality:** Same as D-015 — 98-99 sections parsed.
**File:** `docs/PROGRESS.md:30` says "97 of 174 sections have parsed content"
**Fix required:** Reconcile to "98 of 174 sections parsed" for consistency.
**Status:** OPEN

---

## [ID] D-017 🔴
**Claim (continuation file):** "Two ML systems. First, GSTMind — a legal RAG engine … fine-tuned multilingual embedding model I published on HuggingFace"
**Reality:** The interview story claims the fine-tuned model is used in production. Per D-002, the model IS loaded, but per D-006, the claimed MRR@10 number is fabricated, and per the actual benchmark, the fine-tuned model underperforms the base. The story is internally inconsistent.
**File:** Continuation file's INTERVIEW STORY section
**Fix required:** Rewrite interview story to honestly say: "Production uses base `intfloat/multilingual-e5-small` because the fine-tuned model regressed (catastrophic forgetting on 480 QA pairs covering 12 sections). I published the v1 model on HF as a research artifact with the regression documented."
**Status:** OPEN

---

## [ID] D-018 🟡
**Claim:** "Fine-tuned v2 regressed from 37% to 20% recall"
**Reality:** The base e5-small is 20% (per benchmark_report.json), and the fine-tuned v1 is also 20% (per benchmark — though README at line 449 says "20% (6/30) for v1"). Actually both are 20% based on the file. Need to check if the README's table on line 446-449 is comparing the right things.
**File:** `README.md:446-449`
**Fix required:** Clarify — currently the table shows e5-small at 37% AND v1 at 20%, but the benchmark report shows 20% for whatever was run last. The 37% number was from an earlier run. The current `benchmark_report.json` is the latest. Note this.
**Status:** OPEN (reconcile table with current run)

---

## [ID] D-019 🟡
**Claim:** "Active provider = Template" — see D-005.
**Reality:** Need to check `generation_progress.json` to see if any Claude/DeepSeek batches succeeded. If all batches have at least 1 success in provider counts, then `Template` just means "final fallback" and some rows are LLM-generated.
**File:** `ml/data/generation_progress.json`
**Fix required:** Analyze `completed_batches` array to determine actual provider mix.
**Status:** OPEN (analyze progress)

---

## [ID] D-020 🟡
**Claim:** Backend "GST Classifier: `facebook/bart-large-mnli` + `meet136/indicbert-gst-classifier`"
**Reality:** `backend/app/services/classification_service.py:84` calls BART, line 112 calls `indicbert-gst-classifier` — but the latter is unverified on HF. The trained local model is `muril-gst-v2/`.
**File:** `backend/app/services/classification_service.py:108-127`
**Fix required:** Either verify HF repo exists or change to call local model. Best fix: change to `meet136/muril-gst-classifier-v2` (matching the model name written by `train.py`).
**Status:** OPEN

---

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical (interview/credibility) | 13 |
| 🟡 Accuracy | 5 |
| 🟢 Minor | 0 |
| **Total** | **18 discrepancies** |

The most dangerous are **D-001** (false 37% number), **D-002** (production model mismatch), **D-003** (4 different model names), **D-005** (template vs Claude data), **D-006** (fabricated MRR@10), and **D-017** (inconsistent interview story).

The most easily verified as already correct: **D-007, D-008, D-009, D-010, D-011, D-014, D-015** (numbers about sections, chunks, tables, circulars).
